# dependencies
node_modules/
**/node_modules/

# Expo / React Native
.expo/
**/.expo/
dist/
web-build/
expo-env.d.ts

# generated builds
packages/api/dist/

# local sqlite database — the path is configurable (GOODPINT_DB_PATH) and
# defaults to the working directory, so match anywhere rather than one location.
# It holds password hashes and session rows and must never be committed.
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite
*.sqlite3

# local runtime output
.codex/
*.log

# environment
.env
.env.*
!.env.example

# TypeScript
*.tsbuildinfo

# OS/editor
.DS_Store
Thumbs.db
